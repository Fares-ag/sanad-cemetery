# packages/

Place shared code here, for example:

- `packages/sanad_models` — Dart models shared with admin tools
- `packages/sanad_api` — HTTP client and DTOs

The Flutter app currently lives at the repository root; add `path:` dependencies in `pubspec.yaml` when you create a package.
